# Migration tasks — MUI → shadcn/ui

_Generated by mui-to-shadcn v0.4.1 on 2026-07-06._

## Your task

You are finishing a partial migration from Material UI (MUI) to shadcn/ui in this codebase. The codemod has already converted the mechanical parts and rewritten the imports; your job is to resolve what it could not do automatically:

1. Complete every item under **Open / broken** — each names the file, line, and MUI component, plus what it needs.
2. Then check each item under **Review** — those were converted automatically but may need a fix.

Rules:

- Target stack: shadcn/ui (Base UI variant) + Tailwind CSS v4. Import components from `@/components/ui/*`.
- Never reintroduce `@mui/*`, `@emotion/*`, or `@mui/icons-material` imports. Icons come from `lucide-react`.
- Preserve the original behavior — if a prop was dropped, re-implement its effect rather than lose it.
- Use the public shadcn API: `onValueChange`/`onCheckedChange`, `type="single|multiple"`, `AccordionItem`, etc.
- Base UI specifics: replace `asChild` with the render prop; `checked` is strictly boolean and `value` strictly an array; the Base UI `Select` needs an `items` prop on the root and `nativeButton={false}` on non-button triggers.
- Open each file at the given line and keep edits local; don't reformat unrelated code.

## Install the shadcn components first

```bash
pnpm dlx shadcn@latest add avatar button card dialog input label separator tabs textarea
```

## Open / broken

None — every component the codemod recognized was converted. ✅

## Review — auto-changes to verify (43 notes)

The codemod made these changes automatically. Confirm they behave like the MUI original.

### src/App.tsx

- theme provider removed; port any custom theme tokens (palette/typography/spacing) to your Tailwind/shadcn theme

### src/auth/Signin.tsx

- line 111: nested <InputAdornment> inside the block-converted <TextField> was not adjusted; migrate manually
- line 112: nested <IconButton> inside the block-converted <TextField> was not adjusted; migrate manually
- 4 overlapping edit(s) dropped; an enclosing element was converted as a block

### src/core/Home.tsx

- line 73 <Typography>: color "textSecondary" not mapped

### src/core/Menu.tsx

- line 21 <AppBar>: AppBar position dropped; set it via classes (sticky top-0 / fixed)
- line 23 <Typography>: color "inherit" not mapped

### src/MainRouter.tsx

- MUI styling utilities still present (useTheme); these wrap arbitrary CSS/theme and were left in place — port them manually and remove the @mui/@emotion dependencies

### src/post/Comments.tsx

- line 117 <CardHeader>: CardHeader avatar dropped; compose it manually
- line 119: nested <Avatar> inside the block-converted <CardHeader> was not adjusted; migrate manually
- line 128: nested <TextField> inside the block-converted <CardHeader> was not adjusted; migrate manually
- line 142 <CardHeader>: CardHeader avatar dropped; compose it manually
- line 144: nested <Avatar> inside the block-converted <CardHeader> was not adjusted; migrate manually

### src/post/NewPost.tsx

- line 127 <IconButton>: component dropped; use asChild for polymorphic rendering
- line 104 <CardHeader>: CardHeader avatar dropped; compose it manually
- line 105: nested <Avatar> inside the block-converted <CardHeader> was not adjusted; migrate manually

### src/post/Post.tsx

- line 116 <CardHeader>: CardHeader avatar dropped; compose it manually
- line 116 <CardHeader>: CardHeader action dropped; compose it manually
- line 125: nested <IconButton> inside the block-converted <CardHeader> was not adjusted; migrate manually
- line 118: nested <Avatar> inside the block-converted <CardHeader> was not adjusted; migrate manually
- 2 overlapping edit(s) dropped; an enclosing element was converted as a block

### src/theme.tsx

- MUI styling utilities still present (createTheme); these wrap arbitrary CSS/theme and were left in place — port them manually and remove the @mui/@emotion dependencies

### src/user/DeleteUser.tsx

- line 55 <Dialog>: Dialog onClose -> onOpenChange; the handler now receives a boolean instead of (event, reason)
- line 55 <Dialog>: optional: wrap DialogTitle (and any DialogDescription) in a DialogHeader for shadcn spacing

### src/user/EditProfile.tsx

- line 158 <Button>: Button component -> asChild; the element is wrapped as the single child
- line 148 <Avatar>: Avatar has no fallback content; add initials to AvatarFallback
- line 211: nested <InputAdornment> inside the block-converted <TextField> was not adjusted; migrate manually
- line 212: nested <IconButton> inside the block-converted <TextField> was not adjusted; migrate manually
- 4 overlapping edit(s) dropped; an enclosing element was converted as a block

### src/user/FindPeople.tsx

- line 117 <Avatar>: Avatar has no fallback content; add initials to AvatarFallback
- line 154 <Snackbar>: Snackbar is imperative; use sonner toast() and place <Toaster /> in your layout

### src/user/FollowGrid.tsx

- line 47 <Avatar>: Avatar has no fallback content; add initials to AvatarFallback

### src/user/Profile.tsx

- line 144 <Avatar>: Avatar has no fallback content; add initials to AvatarFallback

### src/user/ProfileTabs.tsx

- line 19 <AppBar>: AppBar position dropped; set it via classes (sticky top-0 / fixed)
- line 20 <Tabs>: Tabs onChange -> onValueChange; the handler now receives the value instead of an event
- line 20 <Tabs>: Tab panels live outside Tabs in MUI; add matching TabsContent elements with a value
- line 20 <Tabs>: Tab without value; TabsTrigger requires a value
- line 20 <Tabs>: Tab without value; TabsTrigger requires a value
- line 20 <Tabs>: Tab without value; TabsTrigger requires a value

### src/user/Signup.tsx

- line 149 <Dialog>: optional: wrap DialogTitle (and any DialogDescription) in a DialogHeader for shadcn spacing
- line 116: nested <InputAdornment> inside the block-converted <TextField> was not adjusted; migrate manually
- line 117: nested <IconButton> inside the block-converted <TextField> was not adjusted; migrate manually
- 4 overlapping edit(s) dropped; an enclosing element was converted as a block
